# Agentic-IDE — project notes for Claude

## Project layout

- **`project.yml`** is the source of truth. The `*.xcodeproj` is generated by
  XcodeGen and is **gitignored** — never edit `AgenticIDE.xcodeproj/project.pbxproj`
  directly; edit `project.yml` and run `xcodegen` (already on PATH locally).
- `Vendor/GhosttyKit.xcframework/` is also gitignored. `Scripts/fetch-ghosttykit.sh`
  pulls a checksum-pinned tarball; the file `Scripts/ghosttykit-checksums.txt`
  is the lockfile.
- App entry: `AgenticIDE/AgenticIDEApp.swift`. The window scene lives in
  `AgenticIDE/Views/MainWindow.swift` and uses `PersistentSplitView`
  (NSSplitViewController-backed) for the three-pane layout.
- Persistence: `~/Library/Application Support/AgenticIDE/projects.json`
  (projects + groups) and `sessions.json` (per-project tab snapshots).

## Working with git

The user's global `CLAUDE.md` forbids direct commits to `main`. Project-specific
addendum: **also never push tags directly that haven't gone through main via PR**.
The release workflow runs on tag push, and a tag pointing at an unreviewed commit
would skip review. Concretely:

```
# always
git checkout -b feat/<short-name>
# … commit, push, gh pr create
gh pr merge <n> --merge --delete-branch
git checkout main && git pull
git tag v0.X.Y && git push origin v0.X.Y    # only after merge
```

If a `gh pr merge` action gets denied by a permission gate, **stop and ask the
user** — don't try to bypass with raw push or rebase tricks.

## Versioning + release

- Tags follow semver `v<MAJOR>.<MINOR>.<PATCH>`. Pushing `v*` triggers
  `.github/workflows/release.yml`.
- The workflow infers `MARKETING_VERSION` by stripping the leading `v` from
  the tag and uses `github.run_number` as `CURRENT_PROJECT_VERSION` (Sparkle
  compares the latter for "is this an upgrade?").
- The workflow:
  1. Runs on `macos-15` (needed for Xcode 16 to read XcodeGen's pbxproj).
  2. `brew install xcodegen` → `xcodegen` → `Scripts/fetch-ghosttykit.sh`
     → `xcodebuild archive` (Release, ad-hoc signed `-`).
  3. `ditto -c -k` the `.app` into `AgenticIDE-<version>.zip`.
  4. Signs the zip with Sparkle's `sign_update` using the
     `SPARKLE_PRIVATE_KEY` repo secret (Ed25519).
  5. Updates `appcast.xml` via `Scripts/update-appcast.sh` and commits it
     back to `main` from the workflow.
  6. Creates a GitHub Release with the zip attached.
- `INFOPLIST_KEY_SUFeedURL`, `INFOPLIST_KEY_SUPublicEDKey`,
  `INFOPLIST_KEY_SUEnableAutomaticChecks`, and `ENABLE_HARDENED_RUNTIME` all
  live under the `AgenticIDE` target in `project.yml`. If a session needs to
  change them, edit the YAML and regenerate with `xcodegen`.

### Cutting a release

1. Confirm `main` is in the state you want to ship.
2. Decide the version. Patch bumps for fixes, minor for features. Read
   `git log --oneline <last-tag>..HEAD` to choose.
3. From `main`:
   ```
   git pull
   git tag vX.Y.Z
   git push origin vX.Y.Z
   ```
4. Watch the workflow:
   ```
   gh run list --workflow=release.yml --limit 1
   gh run watch <run-id> --exit-status
   ```
5. If it fails, fix on a branch, PR, merge, then **delete the tag** (locally
   and on origin) and push it again — the failed run leaves no Release behind
   to clean up because the failure happens before the Release step.
   ```
   git tag -d vX.Y.Z
   git push --delete origin vX.Y.Z
   git tag vX.Y.Z && git push origin vX.Y.Z
   ```

### Sparkle update gotchas

- The Ed25519 private key is in the user's Keychain *and* in the
  `SPARKLE_PRIVATE_KEY` repo secret. They must match. If the secret is empty
  the workflow proceeds with an unsigned appcast item — installed apps will
  reject the update.
- The public key in `project.yml` (`INFOPLIST_KEY_SUPublicEDKey`) must match
  what `generate_keys` printed. Changing the keypair invalidates every
  installed copy — they won't be able to update.
- `appcast.xml` lives at `https://raw.githubusercontent.com/ClarkeFL/Agentic-IDE/main/appcast.xml`
  (set in `INFOPLIST_KEY_SUFeedURL`). If the repo moves or the key URL
  changes, update both `INFOPLIST_KEY_SUFeedURL` and the `update-appcast.sh`
  download URL template.

## Useful commands

```
# regenerate the xcodeproj after editing project.yml
xcodegen

# build locally
xcodebuild -project AgenticIDE.xcodeproj -scheme AgenticIDE -configuration Debug build

# kill + relaunch the dev build
pkill -f AgenticIDE; sleep 1; open ~/Library/Developer/Xcode/DerivedData/AgenticIDE-*/Build/Products/Debug/AgenticIDE.app

# clear stored split-pane positions if PersistentSplitView changes
defaults delete com.fabio.AgenticIDE "NSSplitView Subview Frames AgenticIDE.MainSplit"

# wipe persisted projects + sessions
rm ~/Library/Application\ Support/AgenticIDE/projects.json ~/Library/Application\ Support/AgenticIDE/sessions.json
```

## SourceKit noise

You'll often see SourceKit diagnostics like "Cannot find type 'Project' in
scope" / "'main' attribute cannot be used in a module that contains top-level
code". These are indexer warnings that show up because individual Swift files
reference types from sibling files; they don't reflect real build errors.
**Always run `xcodebuild` to confirm the actual build state** before reacting.
